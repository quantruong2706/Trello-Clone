import styled from 'styled-components';
import { Dialog } from '@mui/material';

const Input = styled.input`
  border: none;
  margin: 20px;
  font-weight: bold;
  font-size: 18px;
  &:focus {
    padding: 8px;
  }
`;

export { Input };
